# LEGACY ALIAS CONFIG
# alias: config/tinyaya_q1_fleurs_overfit_1sample.toml
# canonical: recipes/tinyaya/mimi/overfit/tinyaya_q1_fleurs_overfit_1sample.toml
# This file is generated by scripts/dev/sync_config_aliases.py

# torchtitan Config.toml
# TinyAya + Mimi one-sample overfit smoke test (Q=1)

[job]
dump_folder = "./outputs"
description = "TinyAya Mimi TTS Q1 FLEURS Overfit1"

[profiling]
enable_profiling = false
save_traces_folder = "profile_trace"
profile_freq = 20

[metrics]
log_freq = 1
enable_tensorboard = true
save_tb_folder = "tb"
enable_wandb = true

[model]
name = "CohereLabs/tiny-aya-fire"
num_quantizers = 1
pretrained = true

[optimizer]
name = "AdamW"
lr = 5e-6
eps = 1e-8

[lr_scheduler]
warmup_steps = 10
decay_ratio = 0.2
lr_min = 0.1

[training]
local_batch_size = 1
global_batch_size = 1
seq_len = 1024
max_norm = 1.0
steps = 300
compile = false
dataset = "fleurs_pretok"
dataset_path = "/vol/data/fleurs_pretok_q1"
task = "tts"
max_audio_seconds = 20
mask_text_loss = true
language_tokens = true
languages = ["en"]
overfit_num_samples = 1
sample_generate_every = 10
sample_generate_max_new_tokens = 384
sample_generate_temperature = 0.7
sample_generate_top_k = 30
sample_generate_num_samples = 1
metrics_ema_beta = 0.9

[experiment]
id = "overfit_q1_default"
phase = "overfit_q1"
question = "Can the pipeline overfit one sample without strict hard-fail gates?"
hypothesis = "Default overfit should show rapid loss collapse and emerging generated audio."
owner = "pranav"
tags = ["tinyaya", "tts", "overfit", "q1"]

[tts_eval]
enabled = true
eval_every = 10
asr_model_id = "openai/whisper-small.en"
compute_wer = true
compute_cer = true

[overfit_gate]
require_unconstrained_audio = true
audio_deadline_step = 200
wer_max = 0.35
cer_max = 0.18
frame_ratio_min = 0.60
frame_ratio_max = 1.40
coverage_abs_diff_max = 0.02
min_consecutive_passes = 2
fail_on_no_pass = false

[artifact]
dump_root = ""
save_local_audio_wav = true
save_local_codebook_csv = true
save_local_markdown = true
save_git_snapshot = true

[validation]
enabled = true
dataset = "fleurs_pretok"
local_batch_size = 1
seq_len = 1024
freq = 10
steps = 1

[evaluation]
enable_evaluation = false
evaluation_freq = 500

[parallelism]
data_parallel_replicate_degree = 1
data_parallel_shard_degree = 1
tensor_parallel_degree = 1
pipeline_parallel_degree = 1
context_parallel_degree = 1

[checkpoint]
enable_checkpoint = true
folder = "checkpoint_q1_overfit1"
interval = 20
last_save_model_only = false
export_dtype = "float32"
async_mode = "disabled"
keep_latest_k = 10

[activation_checkpoint]
mode = "none"
selective_ac_option = "op"

[float8]
enable_fsdp_float8_all_gather = false
precompute_float8_dynamic_scale_for_fsdp = false
filter_fqns = ["output"]
